# Git Workflow Rule

- Nach jedem fertiggestellten Änderungsschritt oder Feature werden die Änderungen automatisch mit einer aussagekräftigen Commit-Message committet und direkt zu GitHub (`git push origin main`) gepusht.
